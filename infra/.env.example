# ============================================================
# backend/collab/.env.example
# Copy to .env and fill in values before running locally.
# NEVER commit .env to the repository.
# ============================================================

# PostgreSQL connection string
# Local example: postgresql://collab:secret@localhost:5432/collab_editor
# On Render: injected automatically via fromDatabase
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/collab_editor

# Port the y-websocket server listens on (default: 1234 for local dev)
# On Render: set to 10000 via render.yaml; $PORT is also available
COLLAB_PORT=1234

# JWT secret — must match the FastAPI backend's JWT_SECRET exactly
# Generate a strong value: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=replace-with-a-strong-random-secret-min-32-chars

# UUID of the "system" service account user in the users table
# Used as created_by for auto-snapshots written by the collab server
# Run: INSERT INTO users (email, hashed_password, name) VALUES ('system@internal','n/a','System')
# Then copy the generated UUID here
COLLAB_SYSTEM_USER_ID=00000000-0000-0000-0000-000000000000

# Set to "production" on Render (enables SSL for Postgres)
NODE_ENV=development